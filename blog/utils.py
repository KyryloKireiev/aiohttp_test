def get_add_conflict():
    print("try to add conflict to merge")


# def generate_hash(password):
#     password_bin = password.encode("utf-8")
#     hashed = bcrypt.hashpw(password_bin, bcrypt.gensalt())
#     return hashed.decode("utf-8")
